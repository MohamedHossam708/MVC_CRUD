<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/Css/Style.css">
    <title>MVC Project</title>
</head>
<body>
    <div class="container-flued  d-flex align-items-center  w-100 h-100">
    <div class="w-75 m-auto border  p-2 mt-5">
        <h2 class="text-center my-3">EJS with Node</h2>
        <form action="/addUser" method="post">
            <input type="text"  name="name" class=" my-4 form-control" placeholder="Enter Your Name">
            <input type="email"  name="email" class=" my-4 form-control" placeholder="Enter Your Email">
            <input type="password" name="password" class=" my-4 form-control" placeholder="Enter Your Password">
            <button id="add_btn" class="btn btn-primary"> Add User</button>
        </form>

        <table class="table text-center my-3">
            <thead>
                <th>index</th>
                <th>Name</th>
                <th>Eamil</th>
                <th>Password</th>
                <th>Action</th>
            </thead>
            <tbody>
                
                <% for( let i = 0; i < users.length; i++ ) { %>
                <tr>
                    <td><%= i+1  %></td>
                    <td><%= users[i].name %></td>
                    <td><%= users[i].email %></td>
                    <td><%= users[i].password %></td>
                    <td>
                        <a href="/delete/<%= users[i]._id %>" class="btn btn-danger">Delete</a>
                        <a href="/update/<%= users[i]._id %>" class="btn btn-warning">Update</a>
                    </td>
                    <td></td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>
</body>
</html>